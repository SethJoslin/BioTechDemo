server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
  location /embeddings {
    proxy_pass http://host.docker.internal:8080/embeddings;
  }
  location /umap {
    proxy_pass http://host.docker.internal:8080/umap;
  }
}
